validation_id: runner_test_plan
strategy_experiment: test_strategy
base_config: runner_base_config.yaml
mode: static_is_oos

splits:
  in_sample:
    start_date: "2020-01-02"
    end_date: "2020-06-30"
  out_of_sample:
    start_date: "2020-07-01"
    end_date: "2020-12-30"

decision:
  rules:
    oos_sharpe_min: 0.8
    oos_max_drawdown_max: 0.25
    is_to_oos_sharpe_decay_max: 0.5
    min_oos_trades: 30
    require_positive_oos_total_return: true
  on_review_required:
    - is_to_oos_sharpe_decay_warn: 0.3
